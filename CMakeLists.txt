cmake_minimum_required(VERSION 2.8)
project( lambda )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( lambda lambda.cpp )
target_link_libraries( lambda ${OpenCV_LIBS} )